'use strict';
/**
 * @ngdoc function
 * @name stockApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Manages authentication to any active providers.
 */
angular.module('stockApp')
    .controller('RoleCtrl', function($scope, Auth, $location, $q, Ref, $timeout, tempData) {
        //
    });
